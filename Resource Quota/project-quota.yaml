apiVersion: v1
kind: ResourceQuota
metadata:
  name: project-quota
spec:
  hard:
    configmaps: "10"
    persistentvolumeclaims: "4"
    pods: "2"
    replicationcontrollers: "20"
    secrets: "10"
    services: "10"
    services.loadbalancers: "2"
    requests.cpu: "1"
    requests.memory: 1Gi
    limits.cpu: "2"
    limits.memory: 2Gi
